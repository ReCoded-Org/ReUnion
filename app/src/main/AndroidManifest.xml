<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.safaorhan.reunion">

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LoginSplashScreen">
        <activity android:name=".activity.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/LoginSplashScreen" />

        <activity android:name=".activity.ConversationsActivity"
            android:label="@string/conversationActivityLabel"/>
        <activity android:name=".activity.UsersActivity"
            android:label="@string/usersActivityLabel"/>
        <activity android:name=".activity.ChatActivity" />
    </application>
</manifest>